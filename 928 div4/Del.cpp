#include <iostream>
using namespace std;
#include <vector>
#include <string>
#include <algorithm>
#include <math.h>
#include<map>

#define ll long long
void helper(){
}

int main(){
ios::sync_with_stdio(0);
cin.tie(NULL);
int t;
cin>>t;
map<int ,vector<int> > mp;
for(int i=0;i<t;i++){
    int ele;
    cin>>ele;
    mp[i]={ele};
}
for(int i=0;i<t;i++){
    int ele;
    cin>>ele;
    mp[i].emplace_back(ele);
}
for(int i=0;i<t;i++){
    int ele;
    cin>>ele;
    mp[i].emplace_back(ele);
}
int ct=0;
int time =0;
while(ct<t){
 for(int i=0;i<)
}


}