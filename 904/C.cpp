#include <iostream>
using namespace std;
#include <vector>
#include <string>
#include <algorithm>
#include <math.h>
void helper(vector<long long> &v,vector<pair<long long ,long long >> vec,long long i,long long n){
 if(i<n){
    
 }
}

int main(){
int t;
cin>>t;
for(int j=0;j<t;j++){
    long long n,m;
    cin>>n>>m;
    vector<pair<long long ,long long >> vec;
    vector<long long> v(m);
    for(int i=0;i<n;i++){
        long long l,r;
        cin>>l>>r;
        pair<long long,long long> p;
        p.first=l-1;
        p.second=r-1;
        vec.push_back(p);
    }


}

}